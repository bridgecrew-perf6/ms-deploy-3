apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-fligths.fullname" . }}
  labels:
    {{- include "ms-fligths.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ms-fligths.selectorLabels" . | nindent 4 }}
