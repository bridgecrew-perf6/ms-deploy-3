apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-fligths.fullname" . }}-test-connection"
  labels:
    {{- include "ms-fligths.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-fligths.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
